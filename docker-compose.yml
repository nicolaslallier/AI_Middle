version: '3.9'

# AI_Middle services have been removed
# All middleware functionality is now handled by AI_Infra or moved to other services
#
# Previously contained:
# - auth-service (port 8001)
# - gateway-service (port 8002) 
# - aggregation-service (port 8003)
# - gateway-db (PostgreSQL on port 5433)
#
# These services have been consolidated into AI_Infra infrastructure
# or are being refactored into the application layer.

services: {}

networks:
  default:
    name: ai_middle_network

volumes: {}
